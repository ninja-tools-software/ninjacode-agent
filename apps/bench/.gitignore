dist/
runs/
.tmp/
.tmp-swebench-test/
.cache/
agents.json
# Task fixtures ship a workspace-shaped `.ninjacode/` on purpose; the root rule
# that hides product runtime data would otherwise swallow them.
!tasks/*/fixture/.ninjacode/
!tasks/*/fixture/.ninjacode/**
