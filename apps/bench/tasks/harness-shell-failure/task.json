{
  "id": "harness-shell-failure",
  "description": "Harness: continue after a failing run_shell and fix based on exit",
  "category": "terminal",
  "difficulty": "easy",
  "suites": ["harness"],
  "prompt": "Make check.sh pass and write RESULT.txt with OK",
  "verify": "test \"$(cat RESULT.txt | tr -d '[:space:]')\" = 'OK' && test \"$(cat flag.txt | tr -d '[:space:]')\" = 'ready'",
  "timeoutSec": 60,
  "maxTurns": 10,
  "minToolErrors": 0
}
